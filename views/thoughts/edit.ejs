<%- include("../partials/html-head") %>
<%- include("../partials/nav"); %>
        <h1><%= thought.title %></h1>
        <form action="/thoughts/<%=thought._id%>?_method=PUT" method="POST">
            <fieldset>
                <legend>Thought Name and Main Point(Object)</legend>
                <label for="title">Title:</label>
                <input type="text" name="title" id="title" value="<%= thought.title %>" />

                <label>Object:</label>
                <input type="text" name="object" id="object" value="<%= thought.object %>" />
            </fieldset>
            <fieldset>
                <legend>Thought details</legend>
                <label for="description">Description: </label>
                <textarea name="description" id="description"><%= thought.description %></textarea>
                
                <label for="image">Image Link:</label>
                <input type="text" name="image" id="image" value="<%= thought.image %>" />
            </fieldset>
            <button type="submit"><div class="icon">💡</div>Think</button>
        </form>
    </body>
</html>

