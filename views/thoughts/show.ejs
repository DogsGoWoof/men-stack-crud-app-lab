<%- include("../partials/html-head") %>
    <%- include("../partials/nav"); %>
        <div id="detail-div">
            <h1 id="title-h1"><%= thought.title %></h1>
            <h2 class="object-name"><%= thought.object %></h2>
            <p id="description-p"><%= thought.description %></p>
            <img src="<%= thought.image %>">
        </div>
        <form action="/thoughts/<%=thought._id%>?_method=DELETE" method="POST">
            <button type="submit"><div class="icon"><div id="trashcan">🗑️</div></div>Forget About It</button>
        </form>
    </body>
    <footer>
        <button id="recollect"><a href="../thoughts/<%= thought._id %>/edit">Recollect</a></button>
    </footer>
</html>